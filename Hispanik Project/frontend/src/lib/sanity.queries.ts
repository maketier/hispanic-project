export const homepageQuery = `*[_type == "homepage"][0]{title,heroHeading,body,seo{title,description}}`

export const ecosystemQuery = `*[_type == "ecosystemPage"][0]{title,heroHeading,body,seo{title,description}}`

export const hispanickQuery = `*[_type == "hispanickLabelPage"][0]{title,heroHeading,body,seo{title,description}}`
