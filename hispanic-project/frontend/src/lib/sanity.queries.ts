export const homepageQuery = `*[_id == "homepage"][0]{title,heroHeading,body,seo{title,description}}`

export const ecosystemQuery = `*[_id == "ecosystemPage"][0]{title,heroHeading,body,seo{title,description}}`

export const hispanickQuery = `*[_id == "hispanickLabelPage"][0]{title,heroHeading,body,seo{title,description}}`
